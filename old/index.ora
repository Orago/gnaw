FUNCTION createPerson (name, age) {
	FUNCTION getAge (test, d) {
		RETURN 5;
	};

	SET person         TO OBJECT;
	SET person.name    TO name;
	SET person.age     TO getAge();

	RETURN person;
}

IMPORT cat FROM "./cat.ora";

// PRINT cat(3) & 'cat value';
SET kitty TO cat(3);

SET catList TO ARRAY;

PUSH 'calico', 'siamese', 'ginger' TO catList;